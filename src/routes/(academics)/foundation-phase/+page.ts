import type { PageLoad } from './$types'

export const load: PageLoad = () => {
	return {
		seo: {
			title: 'Foundation Phase',
			description: 'Foundation Phase program focused on early literacy, numeracy, and Christ-centered values.'
		}
	}
}
